export const splitter = "<>";
export const defaultIncomesCategories = `job${splitter}business${splitter}investments${splitter}family${splitter}friends${splitter}family & friends${splitter}games${splitter}others`;
export const defaultExpensesCategories = `food${splitter}health care${splitter}utility${splitter}transportation${splitter}clothing${splitter}others`;
